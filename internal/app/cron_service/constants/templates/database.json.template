{
  "$schema": "{{ .SchemaUrl }}",
  "tables": [
    {
      "name": "tableName",
      "columns": [
        {
          "name": "columnName",
          "type": "string"
        }
      ]
    }
  ]
}
